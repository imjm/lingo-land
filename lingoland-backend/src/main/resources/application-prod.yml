spring:

  datasource:
    url: jdbc:postgresql://postgres/lingoland
    username: postgres
    password: ssafy
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jwt:
    secret: ${JWT_SECRET}

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
openvidu:
  url: http://localhost:4443
  secret: ssafy

# 이미지 저장 시 요청 크기 조절
server:
  tomcat:
    max-http-form-post-size: 5MB

kakao:
  api:
    key: ${KAKAO_REST_KEY}

deepL:
  api:
    key: ${DEEPL_API_KEY}

